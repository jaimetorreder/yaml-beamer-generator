# Core dependencies for minimal working version
pyyaml>=6.0

# Future dependencies (not yet needed):
# jinja2>=3.1.0          # For templating system
# jsonschema>=4.17.0     # For YAML validation
# click>=8.1.0           # For enhanced CLI